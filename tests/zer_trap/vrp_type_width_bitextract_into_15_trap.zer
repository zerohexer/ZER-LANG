// expect-trap
// BUG-974 boundary: a 4-bit extract reaches 15, so into an array of 15 the inline
// bounds check must stay — and here it fires (x = 0xFFFFFFFF, x[3..0] = 15).
u32[15] arr;
u32 pick() { return 0xFFFFFFFF; }
u32 main() {
    u32 x = pick();
    arr[x[3..0]] = 1;
    return 0;
}
