// EXPECTED: compile error
// Rust equivalent: loop { drop(t); } -- use after move on 2nd iteration
// Move struct passed inside loop body — 2nd iteration is use-after-move.

move struct Ticket { u32 num; }

void redeem(Ticket t) {
    u32 n = t.num;
}

i32 main() {
    Ticket t;
    t.num = 7;
    for (u32 i = 0; i < 3; i += 1) {
        redeem(t);   // iteration 0: ok. iteration 1: t already transferred
    }
    return 0;
}
