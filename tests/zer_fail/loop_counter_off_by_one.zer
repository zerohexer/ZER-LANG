// expect-error: loop provably performs an out-of-bounds access
// `i <= 4` on a size-4 array — the classic off-by-one.
u32 main() {
    u32[4] arr;
    for (u32 i = 0; i <= 4; i += 1) { arr[i] = i; }
    return 0;
}
