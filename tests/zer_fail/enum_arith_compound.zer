// expect-error: compound assignment on enum
// BUG-914: the compound spelling forged exactly as the binary one did.
enum Color { red, green, blue }
u32 main() { Color c = Color.red; c += 2; return (u32)c; }
