// Iris theorem: indexing_shape_checks
// Covers row: L06 — bit index out of range
// EXPECTED: compile error

void main() {
    u32 x = 0;
    u32 bit = x[33];  // u32 has 32 bits; index 33 OOB — must be rejected
}
