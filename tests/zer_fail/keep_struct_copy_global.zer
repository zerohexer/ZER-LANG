// EXPECTED: compile error
// keep inference (Site 1): a non-keep pointer param stored into a local struct's
// field, then the struct copied to a global, infers keep on the param (the struct
// carrier propagates the root). Passing &local is rejected at the call site.
struct Box { ?*u32 slot; }
Box g_box;
void stash(*u32 p) { Box local; local.slot = p; g_box = local; }
u32 main() { u32 loc; stash(&loc); return 0; }
