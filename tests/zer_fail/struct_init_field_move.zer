// expect-error: transferred
// BUG-918 (move half): a move struct handed to a designated initializer is
// consumed exactly as `h.t = a;` consumes it, but this sink never reached
// ir_mark_transferred, so the use after it compiled and ran.
move struct Tok { u32 k; }
struct Holder { Tok t; }

u32 main() {
    Tok a;
    a.k = 1;
    Holder h = { .t = a };
    return a.k;
}
