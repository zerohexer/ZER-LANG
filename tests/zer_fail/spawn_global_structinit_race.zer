// EXPECTED: compile error — struct-init field reads non-shared global in spawned worker (data race)
u32 g_counter;
struct Box { u32 v; }
threadlocal u32 sink;
void worker() { Box b = { .v = g_counter }; sink = b.v; }
u32 main() { spawn worker(); g_counter = 5; return 0; }
