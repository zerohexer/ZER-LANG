# Candidate register names for x86_64 — fed to gen_register_tables.sh which
# probes GCC for actual validity. GCC's inline-asm clobber syntax is the
# authoritative oracle: a register is valid iff GCC accepts it as a clobber.
#
# Format: one register name per line. Comments start with #. Blank lines OK.
#
# This is a STARTING SET — F3 expands it to full coverage. Today (F2):
# 64-bit GP, 32-bit GP, 16-bit GP, 8-bit GP, plus rsp/rbp/rip.
# Future: SIMD (xmm0-31, ymm0-31, zmm0-31), x87 FP (st0-7), MMX (mm0-7),
# segment regs (cs/ds/es/fs/gs/ss), control regs (cr0-15), debug regs (dr0-15).

# 64-bit general-purpose
rax
rbx
rcx
rdx
rsi
rdi
rbp
rsp
r8
r9
r10
r11
r12
r13
r14
r15

# 32-bit general-purpose
eax
ebx
ecx
edx
esi
edi
ebp
esp
r8d
r9d
r10d
r11d
r12d
r13d
r14d
r15d

# 16-bit general-purpose
ax
bx
cx
dx
si
di
bp
sp

# 8-bit general-purpose (low byte)
al
bl
cl
dl
sil
dil
bpl
spl

# 8-bit general-purpose (high byte, legacy)
ah
bh
ch
dh

# Instruction pointer (read-only via lea)
rip
