// EXPECTED: compile error
// expect-error: variadic '...' is only allowed on bodyless
// A ZER function with a body cannot be variadic — it would read untyped,
// unverified arguments (the unchecked-boundary ban). Pins the rule stated in
// docs/reference.md "Variadic ... — and how to print".
void g(const *u8 s, ...) { }

u32 main() { return 0; }
