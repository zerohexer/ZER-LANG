// Scope escape: return slice of local array — compile error
// EXPECTED: compile error

[*]u32 get_data() {
    u32[4] local;
    local[0] = 1;
    return local;
}

u32 main() {
    [*]u32 s = get_data();
    return s[0];
}
