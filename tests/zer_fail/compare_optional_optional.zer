// expect-error: cannot compare optional
// BUG-840: `?T == ?T` emitted `a.value == b` — invalid C, caught only by GCC.
u32 main() {
    ?u32 a = 5;
    ?u32 b = 5;
    if (a == b) { return 1; }
    return 0;
}
