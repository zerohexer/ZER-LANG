// expect-error: use after free
// BUG-919 sibling: the universal alloc()/free() pair.
struct Device { u32 id; }
void run() {
    ?*Device mp;
    mp = alloc(Device);
    *Device p = mp orelse return;
    free(p);
    free(p);
}
u32 main() { run(); return 0; }
