// EXPECTED: compile error
// expect-error: @atomic_load on 'g' while it is borrowed
// BUG-1336: parent atomic load
u32 g;
void w(*u32 p) { for (u32 i = 0; i < 2000000; i += 1) { *p += 1; } }
u32 main() { ThreadHandle t = spawn w(&g); u32 x = 0; for (u32 i = 0; i < 2000000; i += 1) { x = @atomic_load(&g); } t.join(); return 0; }
