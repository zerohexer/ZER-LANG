// EXPECTED: compile error
// expect-error: cannot assign to a slice's '.ptr'
// BUG-959: retargeting `.ptr` keeps the old `.len` — the bounds check now covers the wrong memory.
u32 main() { u8[4] a; u8[2] b; [*]u8 s = a; [*]u8 t = b; s.ptr = t.ptr; s[3] = 1; return 0; }
