// expect-trap
// BUG-920: unprovable index in a defer-body FOR initialiser (no guard before).
volatile u32 vi = 100;
u32 g;
u32 main() {
    u32[4] arr;
    u32 i = vi;
    defer { for (u32 k = arr[i]; k < 1; k += 1) { g += 1; } }
    return 0;
}
