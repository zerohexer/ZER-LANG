// EXPECTED: compile error
// expect-error: @critical body re-enables or waits for interrupts
// BUG-1251: interrupts re-enabled inside @critical; the RMW exemption still applied.
volatile u32 cnt;
interrupt TIM2 { cnt = 0; }
u32 main() { @critical { @cpu_enable_int(); cnt += 1; } return 0; }
