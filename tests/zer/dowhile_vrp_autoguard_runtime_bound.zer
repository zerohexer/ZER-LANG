// BUG-748 sibling — same property on do-while, with a runtime bound (see
// while_vrp_autoguard_runtime_bound.zer).
u32 limit() { return 10; }
u32 main() {
    u32 i = 0;
    u32[5] arr;
    u32 n = limit();
    do {
        arr[i] = 99;
        i += 1;
    } while (i < n);
    return 0;
}
