// Bug #6 fix: const Handle cannot be mutated through auto-deref

struct Task { u32 id; }
Pool(Task, 8) tasks;

u32 main() {
    const Handle(Task) h = tasks.alloc() orelse { return 1; };
    h.id = 42;    // COMPILE ERROR — cannot assign through const Handle
    return 0;
}
