// expect-error: arithmetic on enum
// BUG-914: enum + enum can leave the variant set (blue+blue = 4).
enum Color { red, green, blue }
u32 main() { Color c = Color.blue; Color d = c + c; return (u32)d; }
