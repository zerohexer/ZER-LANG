// Iris theorem: arithmetic_typing_rules
// Covers row: M09/M13 — int + float without explicit conversion
// EXPECTED: compile error

void main() {
    u32 a = 5;
    f32 b = 1.0;
    f32 c = a + b;  // mixing int+float — must be rejected
}
