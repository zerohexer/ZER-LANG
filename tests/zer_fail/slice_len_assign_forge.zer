// EXPECTED: compile error
// expect-error: cannot assign to a slice's '.len'
// BUG-959: the slice header is read-only — `.len` writable = any bound forgeable in one line.
u32 main() { u8[4] a; [*]u8 s = a; s.len = 100; s[50] = 1; return 0; }
