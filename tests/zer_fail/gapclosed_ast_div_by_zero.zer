// expect-error: not proven nonzero
// PROMOTED from tests/zer_gaps/ 2026-08-03: this gap is CLOSED and now has a
// regression test. It sat closed-but-untested because nothing executed
// tests/zer_gaps/ — see the gap runner added the same day.
u32 main() {
    u32 a = 100;
    u32 b = 0;  // runtime, not provable at compile time in loop
    for (u32 i = 0; i < 1; i += 1) { b = i; }  // make b unprovable
    return a / b;
}
