// EXPECTED: compile error
// Move struct used after transfer

move struct Token { u32 id; }

void consume(Token t) {
    u32 x = t.id;
}

i32 main() {
    Token t;
    t.id = 42;
    consume(t);
    u32 leak = t.id;
    return 0;
}
