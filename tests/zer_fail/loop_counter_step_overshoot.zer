// expect-error: loop provably performs an out-of-bounds access
// A step > 1 still lands past the end: 0, 3, 6, 9 — 6 and 9 are out of bounds.
u32 main() {
    u32[4] arr;
    for (u32 i = 0; i < 10; i += 3) { arr[i] = i; }
    return 0;
}
