// Shared struct: multi-field grouped update

shared struct State { u32 x; u32 y; u32 total; }
State g_state;

u32 main() {
    g_state.x = 10;
    g_state.y = 20;
    g_state.total = g_state.x + g_state.y;
    if (g_state.total != 30) { return 1; }
    return 0;
}
