// VRP must NOT prove x nonzero from x > -1 — 0 is in [0, INT64_MAX].
i32 main() {
    i32 x = 5;
    x = 0;
    if (x > -1) {
        return 10 / x;  // EXPECTED: compile error - divisor not proven nonzero
    }
    return 0;
}
