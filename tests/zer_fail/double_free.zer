// MUST FAIL: double free on handle
// If this compiles, zercheck is not integrated into the pipeline

struct Task { u32 id; }
Pool(Task, 4) pool;

u32 main() {
    Handle(Task) h = pool.alloc() orelse return;
    pool.free(h);
    pool.free(h);
    return 0;
}
