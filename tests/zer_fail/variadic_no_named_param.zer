// EXPECTED: compile error
// expect-error: '...' requires at least one named parameter before it
// '...' must be the final parameter and at least one named parameter must
// precede it. Pins docs/reference.md "Variadic ... — and how to print".
void f(...);

u32 main() { return 0; }
