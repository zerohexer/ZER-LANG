// zerc-flags: --stack-limit abc
// expect-error: --stack-limit must be a positive integer
// BUG-844: atoi mapped it to 0, which reads as "no limit" — the tool then
// affirms a stack budget it never checked.
u32 main() { return 0; }
