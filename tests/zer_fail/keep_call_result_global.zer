// EXPECTED: compile error
// keep inference (Site 1): a call result that launders a non-keep pointer param
// into a global infers keep on the source param. Passing &local is then rejected
// at the call site. (Was: error at `gk = idfn(p)` demanding keep on p.)
*u32 idfn(*u32 ip) { return ip; }
?*u32 gk = null;
void store(*u32 p) { gk = idfn(p); }
u32 main() { u32 loc; store(&loc); return 0; }
