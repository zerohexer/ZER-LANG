// 3-layer *opaque double-free must be compile error
import opaque_layer2;

u32 main() {
    ?*opaque mh = opaque_layer2.mid_open(21);
    *opaque h = mh orelse { return 1; };
    opaque_layer2.mid_close(h);
    opaque_layer2.mid_close(h);
    return 0;
}
