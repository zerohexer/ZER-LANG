// Const: const pointer param — cannot write through

u32 read_only(const *u32 p) {
    return p[0];
}

u32 main() {
    u32 val = 77;
    u32 r = read_only(&val);
    if (r != 77) { return 1; }
    return 0;
}
