// expect-error: cannot use 'switch' inside a defer body
// BUG-913: compiled clean and shipped _zer_trap("compiler bug: ...") to the user.
u32 g = 0;
u32 main() {
    u32 x = 1;
    defer { switch (x) { 1 => { g = 10; } default => { g = 20; } } }
    return 0;
}
