// EXPECTED: compile error
// expect-error: cannot place a label inside a defer body
// Refactor L: the body is lowered into a template every fire clones; a label's
// block is allocated once outside it, and goto is banned in a defer body anyway.
u32 g = 0;
u32 main() {
    defer { here: g += 1; }
    return 0;
}
