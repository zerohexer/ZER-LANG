// expect-error: use after free
// BUG-915 (slice form): `&s[0]` of a freed heap slice forms an interior pointer
// into freed memory.
void wr(*u32 p) { *p = 7; }
u32 main() {
    [*]u32 s = alloc(u32, 4) orelse { return 1; };
    free(s);
    wr(&s[0]);
    return 0;
}
