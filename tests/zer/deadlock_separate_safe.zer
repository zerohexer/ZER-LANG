// Separate statements via temp — no deadlock
shared struct A { u32 x; }
shared struct B { u32 y; }
A ga;
B gb;
u32 get_b() { return gb.y; }
u32 main() {
    gb.y = 42;
    u32 tmp = get_b();
    ga.x = tmp;
    if (ga.x != 42) { return 1; }
    return 0;
}
