// BUG-1253 positive: an array passed to a [*]u32 spawn parameter is coerced; joined before use.
void w([*]u32 s) { s[0] += 1; }
u32 main() {
    u32[4] a;
    ThreadHandle th = spawn w(a);
    th.join();
    if (a[0] != 1) { return 1; }
    return 0;
}
