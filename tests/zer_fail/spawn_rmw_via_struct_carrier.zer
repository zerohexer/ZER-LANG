// EXPECTED: compile error
// expect-error: non-atomic read-modify-write
// BUG-1046 (RMW REACH): the pointer to `g` reaches the read-modify-write through
// a vehicle the resolvers never followed — a by-value struct CARRIER, at the spawn sink. Accepted on main before the fix
// (measured); a torn read-modify-write that loses an update, silent on bare metal.
volatile u32 g;
struct H { volatile *u32 p; }
void bump(H h) { *h.p += 1; }
void w() { H h; h.p = &g; bump(h); }
u32 main() { spawn w(); u32 x = g; return x & 1; return 0; }
