// EXPECTED: compile error
// BUG-484: Move struct via orelse fallback not tracked

move struct Token { u32 id; }
void consume(Token t) { u32 x = t.id; }

u32 main() {
    ?Token opt = null;
    Token a;
    a.id = 42;
    Token b = opt orelse a;
    consume(a);
    return 0;
}
