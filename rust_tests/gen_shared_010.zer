// Shared struct: @once initialization runs exactly once

u32 init_count;

void do_init() {
    init_count += 1;
}

u32 main() {
    init_count = 0;
    @once { do_init(); }
    @once { do_init(); }
    @once { do_init(); }
    if (init_count != 1) { return 1; }
    return 0;
}
