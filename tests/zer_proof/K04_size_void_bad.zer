// Iris theorem: container_intrinsics_well_typed
// Covers row: K04 — @size invalid for type without defined size
// EXPECTED: compile error

void main() {
    usize s = @size(void);  // void has no size — must be rejected
}
