// EXPECTED: compile error
// keep inference (Site 1): a non-keep pointer param stored into a pointer-param's
// field infers keep on the param. Passing &local is rejected at the call site.
struct Holder { ?*u32 hp; }
void store(*Holder h, *u32 p) { h.hp = p; }
u32 main() { Holder hh; u32 loc; store(&hh, &loc); return 0; }
