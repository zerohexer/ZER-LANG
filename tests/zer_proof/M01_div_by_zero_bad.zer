// Iris theorem: division_safety_via_vrp_or_trap
// Covers row: M01 — division by zero (constant)
// EXPECTED: compile error

u32 main() {
    u32 x = 10;
    u32 y = x / 0;  // literal zero divisor — must be rejected
    return y;
}
