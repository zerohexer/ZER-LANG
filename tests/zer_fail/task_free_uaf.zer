// Task.free() use-after-free — zercheck must catch

struct Task { u32 id; }

u32 main() {
    Handle(Task) t = Task.alloc() orelse { return 1; };
    t.id = 42;
    Task.free(t);
    t.id = 99;         // UAF
    return 0;
}
