// Atomic: @atomic_add and @atomic_load for lock-free counter

u32 counter;

u32 main() {
    counter = 0;
    @atomic_add(&counter, 1);
    @atomic_add(&counter, 1);
    @atomic_add(&counter, 3);
    u32 v = @atomic_load(&counter);
    if (v != 5) { return 1; }
    return 0;
}
