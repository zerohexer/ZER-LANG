// expect-error: use after free
// BUG-977: the orelse spelling of the nested initializer — same hole.
struct T { u32 v; }
struct In { *T p; }
struct H { In inner; }
u32 main() {
    H h = { .inner = { .p = alloc(T) orelse return } };
    free(h.inner.p);
    return h.inner.p.v;
}
