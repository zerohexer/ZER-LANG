// EXPECTED: compile error
// expect-error: needs a WRITABLE backing store
struct T { u32 v; }
const u8[64] cb;
u32 main() {
    Arena a = Arena.over(cb);
    *T t = a.alloc(T) orelse return;
    t.v = 5;
    return t.v;
}
