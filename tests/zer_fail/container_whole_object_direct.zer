// EXPECTED: compile error
// expect-error: address of a whole object
// (from claude/loving-davinci-qo0mm9 afcc7ee1 = vigilant-tesla-ef9cao 021ecaa, body verbatim; 984/985/986 -> 987/988/989)
// BUG-987, second sink: the provenance block used to test `args[0]->kind ==
// NODE_IDENT`, so writing the `&` straight into the call skipped every check.
struct Inner { u32 a; }
struct Outer { u64 pad; Inner in; }
u32 main() {
    Inner i; i.a = 3;
    *Outer o = @container(*Outer, &i, in);
    return (u32)o.pad;
}
