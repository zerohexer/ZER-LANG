// EXPECTED: compile error
// Handle allocated but never freed — leak

Pool(u32, 8) pool;

i32 main() {
    ?Handle(u32) mh = pool.alloc();
    Handle(u32) h = mh orelse return;
    pool.get(h).val = 42;
    return 0;
}
