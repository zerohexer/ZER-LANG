// @size(T) is always positive — division should compile without guard
struct Foo { u32 a; u32 b; u32 c; }
u32 main() {
    usize result = 12 / @size(Foo);
    if (result != 1) { return 1; }  // sizeof(Foo) = 12, 12 / 12 = 1
    return 0;
}
