// EXPECTED: compile error
// expect-error: accesses non-shared global 'gs'
// BUG-1249: a const SLICE global viewing a mutable array was exempt from the race scan.
u32[4] arr;
const [*]u32 gs = arr;
void w() { for (u32 i = 0; i < 100000; i += 1) { u32 x = gs[0]; } }
u32 main() { spawn w(); for (u32 i = 0; i < 100000; i += 1) { arr[0] += 1; } return 0; }
