// VRP range preserved when no pointer aliasing — bounds check skipped
u32[4] arr;

u32 main() {
    u32 idx = 2;
    if (idx >= 4) { return 1; }
    arr[idx] = 42;
    if (arr[2] != 42) { return 1; }
    return 0;
}
