// expect-error: never freed
// BUG-975: an allocation stored straight into a field and never freed was not
// a leak — nothing tracked it.
struct T { u32 v; }
struct H { ?*T p; }
u32 main() {
    H h;
    h.p = alloc(T);
    return 0;
}
