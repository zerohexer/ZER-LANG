// EXPECTED: compile error
// expect-error: non-atomic read-modify-write on volatile global 'g'
// (from claude/loving-davinci-v6o9c5 318bf058; its BUG-975 -> main BUG-999, body verbatim)
// BUG-999: NODE_STRUCT_INIT was an unlisted kind in the (if-chain) RMW walker, so a read
// of `g` inside a struct-literal argument did not count as reading it.
struct W { u32 a; }
volatile u32 g;
W pick(W w) { return w; }
void w() { g = pick({ .a = g }).a + 1; }
u32 main() { spawn w(); spawn w(); return 0; }
