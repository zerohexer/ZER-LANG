// EXPECTED: compile error
// expect-error: address of a whole object
// (from claude/loving-davinci-qo0mm9 afcc7ee1 = vigilant-tesla-ef9cao 021ecaa, body verbatim; 984/985/986 -> 987/988/989)
// BUG-987: an ELEMENT of an Inner[N] is a complete Inner that is nobody's member,
// so subtracting a field offset from it walks out of the array.
struct Inner { u32 a; }
struct Outer { u64 pad; Inner in; }
u32 main() {
    Inner[3] arr;
    *Inner p = &arr[1];
    *Outer o = @container(*Outer, p, in);
    return (u32)o.pad;
}
