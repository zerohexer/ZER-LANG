// EXPECTED: compile error
// expect-error: address of a whole object
struct L { u32 x; }
struct D { u32 id; L link; }
u32 main() {
    *L h = alloc(L) orelse return;
    *D d = @container(*D, h, link);
    u32 r = d.id;
    free(h);
    return r;
}
