// Enum: enum with explicit values

enum Status { ok = 0, not_found = 404, error = 500 }

u32 main() {
    Status s = Status.not_found;
    switch (s) {
        .ok => { return 1; }
        .not_found => { return 0; }
        .error => { return 2; }
    }
}
