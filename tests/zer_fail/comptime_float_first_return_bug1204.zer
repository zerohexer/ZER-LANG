// EXPECTED: compile error
// expect-error: must be a single 'return <expr>;'
// BUG-1204: the float sibling — D(0.5) folded 1.0 (the then-branch) instead of 2.0.
comptime f64 D(f64 x) { if (x > 1.0) { return 1.0; } return 2.0; }
u32 main() { f64 a = D(0.5); if (a != 2.0) { return 1; } return 0; }
