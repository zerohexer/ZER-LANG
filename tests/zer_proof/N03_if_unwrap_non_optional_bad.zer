// Iris theorem: unwrap_requires_optional
// Covers row: N03 — if-unwrap requires optional
// EXPECTED: compile error

void main() {
    u32 x = 5;
    if (x) |v| {  // x is u32, not optional — must be rejected
        return;
    }
}
